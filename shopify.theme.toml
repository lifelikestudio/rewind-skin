[environments.live]
store = "rewind-skin-co"

[environments.live-release]
store = "rewind-skin-co"
nodelete = true
ignore = [
  "config/settings_data.json",
  "templates/*.json",
  "templates/customers/*.json",
  "sections/*.json",
]

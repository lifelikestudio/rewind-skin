Build a Shopify 2.0 Liquid section from a specced component in the Rewind Skin V3 component inventory.

Read `prompts/build-section.md` for the full workflow, then execute it for the component the user specifies.

The user will name a component from `docs/component-inventory.md`. Build the section using design system tokens, validate with theme check.

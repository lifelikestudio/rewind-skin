Extract design values from a V3 Figma frame for the Rewind Skin design system.

Read `prompts/extract-tokens.md` for the full workflow, then execute it.

The user will provide a Figma URL or node ID for a full-page frame. Start with `get_metadata` to read the hierarchy, present the top-level structure, and ask what component to extract. Follow the responsive and state extraction workflow in the prompt.
